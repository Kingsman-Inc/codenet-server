[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["pip install -r Core/requirements_v3.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd Core && gunicorn codenet_server_v3:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
