[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "cd Core && python -m gunicorn codenet_server_v3:app --bind 0.0.0.0:${PORT:-8000} --workers 2 --timeout 120"
